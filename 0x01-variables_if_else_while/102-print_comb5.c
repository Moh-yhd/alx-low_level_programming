#include <stdio.h>

/**
 * main - createa every possible comibations of two digit numbers"
 *
 * Return: returns zero if succcess
 */

int main(void)
{
	int i = 48;

	while (i >= 48 && i <= 57)
	{
		putchar(i);
		i = i + 1;

	}
	return(0);
}
